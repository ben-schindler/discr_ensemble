# Discriminator Ensemble

A library for creating and managing ensembles of discriminator models.

## Installation

The recommended installation method is via conda:

```bash
conda env create -f environment.yml
```

## Overview

Discriminator Ensemble provides tools for creating, training, and evaluating ensembles of discriminator models. This approach can improve robustness and performance across a variety of adversarial implementations.
